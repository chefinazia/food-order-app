import classes from '../app.module.css';

const Footer = () => {
    return (
            <div className={classes.footer}>
                    Footer Content
            </div>

    )
}

export default Footer;